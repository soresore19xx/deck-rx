# Build output
com.hogehoge.deck-rx.sdPlugin/bin/
node_modules/

# Plugin runtime logs
com.hogehoge.deck-rx.sdPlugin/logs/
# Some test / dev runs land an extra logs/ at repo root depending on cwd
logs/

# Local config (per-user, contains host/device-specific values)
com.hogehoge.deck-rx.sdPlugin/config.json
com.hogehoge.deck-rx.sdPlugin/config.json.*

# User's preset store (populated by the SDR++ Import button + hand edits;
# host-specific so it must stay out of the shared repo)
com.hogehoge.deck-rx.sdPlugin/data/presets.json
com.hogehoge.deck-rx.sdPlugin/data/presets.json.*

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

2026-*.txt
*.2026-*

# Claude Code /export session transcripts
conversation-*.txt
[0-9][0-9][0-9][0-9]-*-this-session-is-being-continued-from-a-previous-c*.txt

# Claude Code permission-mode startup logs
**/*-permission-mode.txt

# Claude Code per-project local state (scheduled tasks lock, local settings)
.claude/

# Personal developer notes (host paths / incident logs — keep out of public repo)
docs/dev-notes.md

# Project-local AI assistant guidance (kept untracked for public repo)
/CLAUDE.md

# Naudiodon / native-binding segfault dumps (segfault-handler writes here)
/crash.log
native/samplerate/build/


# Companion app build artefacts (mac-app/build-app.sh regenerates them)
mac-app/deck-rx
mac-app/deck-rx.icns
mac-app/deck-rx.iconset/
